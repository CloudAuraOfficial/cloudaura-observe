global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alerts.yml"

scrape_configs:
  # Prometheus self-monitoring
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  # P1 — cloudaura-rag (FastAPI)
  - job_name: "cloudaura-rag"
    metrics_path: /metrics
    static_configs:
      - targets: ["localhost:8001"]
        labels:
          project: "P1"
          service: "cloudaura-rag"
          stack: "fastapi"

  # P2 — cloudaura-slm (FastAPI)
  - job_name: "cloudaura-slm"
    metrics_path: /metrics
    static_configs:
      - targets: ["localhost:8002"]
        labels:
          project: "P2"
          service: "cloudaura-slm"
          stack: "fastapi"

  # P5 — cloudaura-voice (FastAPI)
  - job_name: "cloudaura-voice"
    metrics_path: /metrics
    static_configs:
      - targets: ["localhost:8005"]
        labels:
          project: "P5"
          service: "cloudaura-voice"
          stack: "fastapi"

  # P6 — aura-platform (.NET / prometheus-net)
  - job_name: "aura-platform"
    metrics_path: /metrics
    static_configs:
      - targets: ["localhost:8006"]
        labels:
          project: "P6"
          service: "aura-platform"
          stack: "dotnet"

  # P6 — aura-platform Worker (prometheus-net, metrics port)
  - job_name: "aura-platform-worker"
    metrics_path: /metrics
    static_configs:
      - targets: ["localhost:9091"]
        labels:
          project: "P6"
          service: "aura-worker"
          stack: "dotnet"

  # Node metrics (host machine)
  - job_name: "node"
    static_configs:
      - targets: ["localhost:9100"]
